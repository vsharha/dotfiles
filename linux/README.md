# Linux

Shared Linux setup lives here. Distro-specific setup should stay in a
subdirectory such as `arch/`.

Run the general bootstrap from the repo root:

```bash
./linux/bootstrap.sh
```

Link Linux configs and home files:

```bash
./linux/symlinks.sh
```

